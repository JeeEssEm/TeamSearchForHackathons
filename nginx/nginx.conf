worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /tg_bot/ {
            proxy_pass http://tg_bot:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /admin/ {
            proxy_pass http://admin:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
